.env
venv/
__pycache__/
*.pyc
*.pyo
*.db
*.sqlite3
# Pre-migration database snapshots. *.db above does not match these — they
# carry real patient data and must never be committed.
*.db.bak
*.db.*.bak
clinic.db.*
.DS_Store
CLAUDE.md
.claude/
uploads/
# Duplicate local checkout of this repo — has its own .git, must never be
# added as an embedded gitlink by `git add -A`
Clincos/
